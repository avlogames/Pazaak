// if numb = 0

// reduce,

// 1, (2 size combo ) => inject into array.

// if winning it's good.

function smallestWinningScore(needToWinOrTie, sideDeckCards, greedy) {
  // all combos that are 17 or more upto 20

  const cards = sideDeckCards.reduce((val, acc) => {

  }, [])

  if (greedy) {
    // higest score from remaining.
    // map over and take shortest length
  } else {
    // map over and take shortest length
    // higest score from remaining.
  }

  return finalCards

}
