import { responsiveFontSize, responsiveHeight, responsiveWidth } from "react-native-responsive-dimensions"
export const rFont = (size) => responsiveFontSize(size)
export const rHeight = (height) => responsiveHeight(height)
export const rWidth = (width) => responsiveWidth(width)
